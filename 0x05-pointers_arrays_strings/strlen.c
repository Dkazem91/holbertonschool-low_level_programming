/**
 * _strlen - length of string
 *
 * @s: string to get length
 * Return: length number with nullterminator
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}
